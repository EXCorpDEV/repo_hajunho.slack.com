<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"> <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/foodtf"
        android:label="@string/app_name"
        android:roundIcon="@drawable/foodtf"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecognizeFoodWithTFLite"
        tools:replace="android:label, android:icon, android:theme"> <activity
        android:name="de.inovex.recognizefoodwithtflite.MainActivity"
        android:exported="true"
        android:theme="@style/SplashScreenTheme">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    </application>

</manifest>